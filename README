Databreeze API Server

The Databreeze database API server responds to ajax requests from the Databreeze client and queries and updates a mongodb server. The server utilizes nodejsand expressjs to handle web requests/responses and mongoose to query the mongodb database server. 